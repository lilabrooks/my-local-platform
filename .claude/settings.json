{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",

  "//": "Shared project settings. Personal overrides belong in .claude/settings.local.json, which is gitignored. This file scope is deliberately limited to MCP server enablement -- command permissions are a separate decision and are left to each user.",

  "enableAllProjectMcpServers": true
}
