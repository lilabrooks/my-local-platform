apiVersion: v1
kind: Service
metadata:
  name: relay-deliver
  labels:
    app.kubernetes.io/name: relay-deliver
    app.kubernetes.io/part-of: my-local-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: relay-deliver
  ports:
    - name: http
      port: 80
      targetPort: http
