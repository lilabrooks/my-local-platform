# M4 #96 staging evidence, 2026-09-20

Status: staging complete; evidence PR merge closes #96. #97's paid execution
remains unapproved. No hourly runtime was applied.

Frozen source: `474dca7e8e121c08f2951b02871cfe4c4b87e2ee` (PR #147).
Local run: `20260920T153931Z`. AWS run: `20260920T155738Z`, `us-east-1`.
The candidate checkout remained clean; documentation and publication used a
separate checkout. The owner approved the new qualification explicitly;
remote `main` matched at the final handoff and the merge hold ended at
15:57:38Z. The existing cheap-tier authority covered the reviewed apply.

## Qualification and recovery

The [rehearsal record](m4-replay-capture-rehearsal.md#completed-bounded-qualification-on-2026-09-20-utc)
and [qualification manifest](../evidence/m4-local/20260920T153931Z/qualification.json)
retain first-attempt passes, exact-SHA receipt/export hashes and visual review.
The two diagnosed setup refusals are recorded there: absent private `.env`
before load, and price commands started before preflight completed. Each had
one correction and recheck. No source or test change, additional load cohort, receipt
rewrite, or gate weakening was used to reach staging.

## Actual AWS sequence

All Make commands below used `AWS_RUN_ID=20260920T155738Z` and
`AWS_APPROVED_COMMIT=474dca7e8e121c08f2951b02871cfe4c4b87e2ee`.

1. `make aws-account-check` passed identity, backend, project budget, quotas,
   regional availability and EKS 1.35 standard-support gates. The persistent
   monthly budget remains $5, project-filtered before tax, with three verified
   subscribers/notifications. This does not establish historical allocation
   of tax or unallocated account charges.
2. Official pricing pages and fresh AWS Price List API responses supplied
   `make aws-prices`. The worksheet totals $1.0219/hour for the fixed topology,
   below $1.25/hour. The packet retains source links and timestamps.
3. `make aws-plan` with `enable_eks=false`, `enable_msk=false` and
   `enable_rds=false` produced 12 creates, zero updates and zero deletions:
   two ECR repositories and lifecycle policies; the artifact bucket, lifecycle
   and public-access block; SNS topic/subscription; two SQS queues and a policy.
   Review found no hourly resource. `make aws-up` applied that exact saved plan
   from 16:06:59Z to 16:08:31Z, including its private state backup.
4. `make aws-inventory-empty` passed. `make aws-stage-images` built and pushed
   relay and sink for `linux/amd64` with immutable full-SHA tags. Remote
   digests, OCI revisions and repository settings passed inspection.
5. `make aws-plan` with all three hourly flags true and the actual operator
   IPv4 `/32` produced the reviewed hourly plan. It has 82 creates, 12 data
   reads and 12 no-ops; no update, replacement or deletion. The hourly shape is
   one EKS cluster, one MSK Serverless cluster, one private encrypted RDS
   `db.t4g.micro` with 20 GB gp3, one NAT gateway, and one Spot `t3.medium`
   node group with two desired nodes (one minimum, three maximum).
   The plan gate verified 14 tagged resources and instance, volume and
   network-interface tags in the EKS launch template. This plan was not applied.
6. Final `make aws-inventory-empty` at 16:13:47Z found the two ECR repositories
   and zero EKS, MSK, RDS, EC2, EBS, NAT, Elastic IP, load balancer and project
   log resources. The cheap dev resources, backend and persistent budget
   intentionally remain staged; this is not a dev teardown receipt.
7. `make aws-go-no-go` passed at 16:13:51Z, binding the source, eight inputs,
   immutable image digests, plan hash, topology, cost limits, cleanup owner,
   bootstrap/capture sequence and abort command.
8. `m4-publication-extra.py stage`, then `verify-stage`, passed against the
   [sanitized staging packet](../evidence/m4-staging/20260920T155738Z/publication.json).
   Its `execution_authorized: false` preserves the separate #97 approval gate.

## Preserved private artifacts

Raw archive root: `.evidence/m4/20260920T155738Z/`. The plan binaries and
exports are retained privately under the archive's `.terraform/` directory.
No raw plan, state, account ID, subscriber address or operator IP is published.
GO and all eight raw inputs were preserved before any later refresh. The
publication manifest binds their raw and sanitized hashes; the additional
records below establish the cheap apply and separate saved-plan review.

| Relative file | SHA-256 |
|---|---|
| `03-plan-summary-cheap.json` | `c36ffdd20d43420dfc2d466626d8a9d25fa237f4e6500a9b15a2d49617b2ddd3` |
| `cheap-apply-command-result.json` | `0e474b63121a56f886e2a80d44150277d762c703a1c85f105acb0332cf8b54d4` |
| `cheap-apply-command.log` | `d59fe2452c13e868a330c6daa6a0ba369f0865a19598a6473dc9407124f5f419` |
| `hourly-plan-review.json` | `eae6f8f1705528c6a7c8a8834bcda71b6d150e1fb18057c1fdcc5608e6d13cd1` |
| `final-inventory-command-result.json` | `6ecbfef193ac322d3cc153d6417d96ad16ad0ae5622e1f756253dbff9705162f` |
| `stage-images-command-result.json` | `7d3c21edb9cdfd8bfcd54da070522d1c05d827fd2656e7630fff425c5ae03ad6` |

## Closure and later execution

Merge the evidence PR with `Closes #96`; no further rehearsal, paid capture,
paid screenshot or settled bill is needed to close staging. Keep #97 waiting
for explicit paid-run authority. This historical GO proves what passed at its
recorded time; it is not perpetual approval or a promise of future freshness.

For #97, retain this frozen source and refresh only expired or changed AWS
observations and their dependent plan/GO inputs. Later documentation-only
merges do not invalidate this local qualification. A deliberate runtime source
change requires a new candidate decision; it is not an automatic consequence
of merging this publication.
